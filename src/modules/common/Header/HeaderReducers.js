// Import Actions
import {
    // ADD_POST
} from './HeaderActions'

// Initial State
const initialState = {
    data: []
}

// Reducer
const HeaderReducers = (state = initialState, action) => {
    switch(action.type) {
        
    }
}

export default HeaderReducers